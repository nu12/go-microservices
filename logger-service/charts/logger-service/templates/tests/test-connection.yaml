apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "logger-service.fullname" . }}-test-connection"
  labels:
    {{- include "logger-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "logger-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
