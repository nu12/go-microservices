apiVersion: v1
kind: Service
metadata:
  name: {{ include "mailer-service.fullname" . }}
  labels:
    {{- include "mailer-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mailer-service.selectorLabels" . | nindent 4 }}
